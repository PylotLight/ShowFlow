apiVersion: v1
kind: Namespace
metadata:
  name: showflow

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: showflow-data
  namespace: showflow
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: showflow
  namespace: showflow
  labels:
    app: showflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showflow
  template:
    metadata:
      labels:
        app: showflow
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
        - name: showflow
          image: ghcr.io/pylotlight/showflow:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
            - name: GITHUB_REPO
              value: "PylotLight/ShowFlow"
            # Enable memory diagnostics — BlackholeClient.mem() logs rss/heap/
            # cgroup per file-hash/oracle/move step so OOM spikes during the
            # watch-folder scan are traceable from pod logs.
            - name: SHOWFLOW_DEBUG
              value: "true"
          volumeMounts:
            - name: data
              mountPath: /data
            - name: host-data
              mountPath: /Data
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /internal/ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 4Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: showflow-data
        - name: host-data
          hostPath:
            path: /Data
            type: DirectoryOrCreate

---
apiVersion: v1
kind: Service
metadata:
  name: showflow
  namespace: showflow
  labels:
    app: showflow
spec:
  type: ClusterIP
  selector:
    app: showflow
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
